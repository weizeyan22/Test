Users_Workgroups = 
VAR UniqueUsers =
    DISTINCT(
        UNION(
            SELECTCOLUMNS('BR', "User", 'BR'[Latest WorkItem Owner]),
            SELECTCOLUMNS('BR', "User", 'BR'[All Members])
        )
    )
RETURN
    UniqueUsers
